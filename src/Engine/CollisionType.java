package Engine;

/**
 * Indicates the type of collision that a player should have when it collides with
 * a given entity.
 */
public enum CollisionType {
    DEFAULT,INSTANT_DEATH,DAMAGE,PREVENT_JUMP
}
